# ============================================
# n8n LifeCoach - Environment Configuration
# ============================================
# 
# WICHTIG: Diese Datei in .env kopieren und die folgenden Werte ANPASSEN:
# - N8N_HOST: Deine Subdomain oder Server-IP mit Port
# - N8N_ENCRYPTION_KEY: Ein sicherer, zufälliger Schlüssel (mind. 32 Zeichen)
# - DB_POSTGRESDB_PASSWORD: Ein sicheres Datenbank-Passwort
# - N8N_PROTOCOL: https (für Produktion) oder http (nur für Tests)

# ============================================
# n8n Configuration
# ============================================
# Externe URL (Host) - MUSS angepasst werden!
# Beispiel: n8n-lc.example.com oder 123.456.789.0:5679
N8N_HOST=N8N_LC_HOST

# Interner Port (Standard: 5678)
N8N_PORT=5678

# Protokoll: https (Produktion) oder http (nur für Tests)
N8N_PROTOCOL=https

# Encryption Key - MUSS geändert werden!
# Generiere einen sicheren Schlüssel mit: openssl rand -base64 32
N8N_ENCRYPTION_KEY=CHANGE_ME_LIFECOACH_KEY

# ============================================
# PostgreSQL Database Configuration
# ============================================
# Datenbankname
DB_POSTGRESDB_DATABASE=n8n_lc

# Datenbank-Benutzer
DB_POSTGRESDB_USER=n8n_lc

# Datenbank-Passwort - MUSS geändert werden!
DB_POSTGRESDB_PASSWORD=n8n_lc_password
